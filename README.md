# Booker dark mode demo

1. Create env vars file
    ```
    NEXT_PUBLIC_CAL_CLIENT_ID=""
    NEXT_PUBLIC_CAL_USERNAME=""
    NEXT_PUBLIC_CAL_EVENT_SLUG=""
    ```
1. Install project
    ```
    npm install
    ```

1. Run dev server
    ```
    npm run dev
    ```
